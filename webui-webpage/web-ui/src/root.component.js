import React from "react";
import { createRoot } from "react-dom/client";
import { HelloIgnition } from "./index";

const root = createRoot(document.getElementById("root"));
root.render(<HelloIgnition />);
